import { DirCcRolloverDirective } from './dir-cc-rollover.directive';

describe('DirCcRolloverDirective', () => {
  it('should create an instance', () => {
    const directive = new DirCcRolloverDirective();
    expect(directive).toBeTruthy();
  });
});
